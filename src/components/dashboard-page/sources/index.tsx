import Window from "../window";
import Sources from "./sources";

/** The sources tab: the source list inside the standard window card. */
const SourcesTab = () => (
  <Window className="flex-1">
    <Sources />
  </Window>
);

export default SourcesTab;
